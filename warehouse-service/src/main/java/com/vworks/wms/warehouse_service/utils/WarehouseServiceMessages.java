package com.vworks.wms.warehouse_service.utils;

import com.vworks.wms.common_lib.utils.MessageUtil;

public interface WarehouseServiceMessages extends MessageUtil {
    String OK = "OK!";
    String ERRORS = "Danh sách lỗi";
    String REQUEST_INVALID = "Dữ liệu đầu vào không hợp lệ";
    String FIELD_FORMAT = "Trường không đúng định dạng";
}
